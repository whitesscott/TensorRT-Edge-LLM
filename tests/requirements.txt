pytest>=7.0.0
pytest-timeout>=2.1.0
pytest-xdist>=3.0.0
pytest-html>=3.1.0
parameterized>=0.9.0
pyyaml>=6.0
psutil>=5.9.0
packaging>=24.2
pillow>=12.2.0
logbar>=0.1.7
accelerate>=1.10.1
threadpoolctl>=3.6.0
tokenicer>=0.0.5
pypcre>=0.2.5
device-smi>=0.5.1
optimum==2.1.0
nvidia-modelopt==0.44.0
# ONNX export toolchain: the few-layer validation runs `export` inside
# the pipeline-test runner, unlike other pipeline tests which reuse the ONNX
# pre-exported by the checkpoint_export job. This template installs only
# tests/requirements.txt (not the top-level requirements.txt nor `pip install
# -e .`), so these must be pinned here too (kept in sync with requirements.txt).
onnx==1.19.0
onnxscript==0.7.0
onnx-graphsurgeon==0.6.1
fastapi==0.136.1
httpx>=0.27.0
